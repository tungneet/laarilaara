# Copy to .env.local for local/manual deployment testing. NEVER commit .env.local.
# These are secrets — do not put real values in any committed file.

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
